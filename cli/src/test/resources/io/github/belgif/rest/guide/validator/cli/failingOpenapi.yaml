openapi: 3.0.0
info:
  title: My First Api
  description: all very fancy
  version: v1
servers:
  - url: /demo/v1
paths:
  /myFirstPath:
    get:
      responses:
        200:
          description: my response
          content:
            application/json:
              schema:
                type: object