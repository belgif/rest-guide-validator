---
swagger: "2.0"
info:
  description: testcase
  version: "1.0"
  title: testCase
basePath: /REST/testCase/v1
schemes:
  - https
paths: {}
definitions:
  problem:
    type: object
responses:
  problemResponse:
    description: a problem
    schema:
      $ref: "#/definitions/problem"
parameters:
  myParam:
    name: enterpriseNumber
    in: body
    description: Enterprise Number
    required: true
    schema:
      $ref: '#/definitions/problem'
