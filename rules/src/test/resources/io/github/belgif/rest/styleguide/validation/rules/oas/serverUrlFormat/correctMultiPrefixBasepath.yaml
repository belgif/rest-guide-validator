openapi: 3.0.1
info:
  title: correctMinimalBasepath
  version: "1.0"
servers:
  - url: '/REST/multipleOther/paths/should/stillWork/v1'
paths:
  /myFirstPath:
    get:
      operationId: myOperation
      responses:
        default:
          description: Good response
          content:
            application/json:
              schema:
                type: string