openapi: 3.0.4
info:
  title: Referenced Security Schemes Integration Test
  description: An integration test to validate the correct parsing of the security schemes over multiple files.
  version: 1.0.0
servers:
  - url: /demo/v1
paths:
  /health:
    $ref: "common.yaml#/paths/~1health"
components:
  securitySchemes:
    OauthHealthCommon:
      $ref: "common.yaml#/components/securitySchemes/OauthHealthCommon"