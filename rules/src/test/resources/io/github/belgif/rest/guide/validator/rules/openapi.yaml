openapi: 3.0.1
info:
  title: PresenceOfPayloadFailingOpenApi
  version: '1.0'
servers:
  - url: '/api/v1'
paths:
  /health:
    get:
      operationId: health
      summary: health endpoint
      responses:
        '200':
          description: Very good 200
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pet"
        503:
          description: Not so good 503
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pet"
        default:
          description: unexpected error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /everythingIsGoodHere:
    get:
      summary: This path is completely compliant with the guidelines
      description: Nice path
      operationId: getEverythingIsGoodHere
      responses:
        '200':
          description: Very good 200
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pet"
        default:
          description: unexpected error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    post:
      summary: post where everything is good
      operationId: alsoVeryGoodButItsPost
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Pet"
      responses:
        '201':
          description: Very good 201
          content:
            application/json:
              schema:
                type: object
                title: Response
        default:
          description: unexpected error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
components:
  requestBodies:
    RequestBodyWithSchema:
      required: true
      content:
        application/json:
          schema:
            type: object
            title: RequestBodyWithSchema
  schemas:
    Pet:
      type: "object"
      required:
        - id
        - name
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        tag:
          type: string
    Pets:
      type: array
      items:
        $ref: '#/components/schemas/Pet'
    Problem:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: integer
          format: int32
        message:
          type: string
