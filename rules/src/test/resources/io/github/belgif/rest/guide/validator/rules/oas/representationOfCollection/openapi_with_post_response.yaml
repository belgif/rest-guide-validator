openapi: 3.0.1
info:
  title: RepresentationOfCollection
  version: '1.0'
servers:
  - url: '/api/v1'
paths:
  /myPath:
    post:
      summary: yes
      operationId: post
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                - name:
                    type: string
      responses:
        200:
          content:
            application/json:
              schema:
                type: string