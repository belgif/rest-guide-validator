openapi: 3.0.1
info:
  title: URI_SHOULD_NOT_contain_a_file_extension
  version: '1.0'
servers:
  - url: '/api/v1'
paths:
  /myPath.txt:
    get:
      operationId: getPath
      responses:
        '200':
          description: Very good 200
          content:
            application/json:
              schema:
                type: string
