openapi: 3.0.1
info:
  title: TestCase
  version: '1.0'
servers:
  - url: '/api/v1'
paths: {}
components:
  schemas:
    AllOfComplicated: # this is valid
      allOf:
        - allOf:
            - type: string
        - oneOf:
            - type: string  # violates sch-oneOf, but not this rule
            - type: object
            - description: "..." # any type