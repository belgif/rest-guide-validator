openapi: 3.0.0
info:
  title: generalDocumentation
  version: 1
paths:
  /myFirstPath:
    get:
      operationId: operationId
      summary: operation summary
      responses:
        default:
          content:
            application/json:
              schema:
                type: object
components:
  schemas:
    MyFirstSchema:
      allOf:
        - $ref: "#/components/schemas/Parent"
        - type: object
          description: description, no title required
          properties:
            childName:
              type: object
              title: child name
    Parent:
      type: object
      description: description, no title required
      properties:
        parentName:
          type: string
