openapi: 3.0.0
info:
  title: generalDocumentation
  version: 1
paths:
  /myFirstPath:
    get:
      operationId: operationId
      responses:
        default:
          content:
            application/json:
              schema:
                type: object

